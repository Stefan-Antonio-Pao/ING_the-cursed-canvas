# Version Bump to v3.0.1

Date: 2026-07-07

## Research

- Searched project version references and found the app version in `package.json`.
- Confirmed `package-lock.json` mirrors the root package version in the top-level metadata and root package entry.
- Confirmed other `3.0.0` references in `package-lock.json` are dependency versions or dependency ranges and should not be changed.

## Plan

- Update the desktop package version from `3.0.0` to `3.0.1`.
- Keep dependency versions untouched.
- Verify the package metadata and diff after the update.

## Implementation

- Ran `npm version 3.0.1 --no-git-tag-version` so npm updated `package.json` and the root lockfile metadata consistently.

## Check

- Validate `package.json` as JSON.
- Confirm npm reports `3.0.1` as the root package version.
- Review the diff to ensure only version metadata and this log changed.

## Log

- Updated project version metadata for release tag `v3.0.1`.
